---
description: Full vertical slice for a resource — migration → types → service → routes
argument-hint: <resource-name>
---

Invoke the `casting-a-feature` skill via the Skill tool. Pass `$ARGUMENTS` as the resource name (lowercase singular). If `$ARGUMENTS` is empty, ask the user for it.
