---
description: Bootstrap a brand-new magic-based microservice
argument-hint: <service-name>
---

Invoke the `casting-a-new-service` skill via the Skill tool. Pass `$ARGUMENTS` as the requested service name. If `$ARGUMENTS` is empty, invoke `divining-intent` first to collect the service name and wizard answers.
