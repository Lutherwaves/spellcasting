---
description: Service layer only (types + features pkg, no HTTP)
argument-hint: <resource-name>
---

Invoke the `casting-a-service-layer` skill via the Skill tool. Pass `$ARGUMENTS` as the resource name. If empty, ask.
