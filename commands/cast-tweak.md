---
description: Modify an existing resource — add field, filter, guard, soft-delete, swap adapter
---

Invoke the `tweaking-a-cast` skill via the Skill tool. The skill asks the user what they want to change and applies the DTO-completeness rule on every field-touching change.
