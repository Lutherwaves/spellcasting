---
description: Interactive wizard — asks what you're building and proposes a cast sequence
---

Invoke the `divining-intent` skill via the Skill tool. Pass any user-provided context (`$ARGUMENTS` if non-empty) as the initial brief; otherwise start from Q1.
